<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                      http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.qcadoo.mes</groupId>
		<artifactId>mes</artifactId>
		<version>0.1-SNAPSHOT</version>
	</parent>

	<artifactId>mes-application</artifactId>
	<packaging>war</packaging>

	<name>Qcadoo MES :: Application</name>

	<dependencies>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>mes-plugins-crud</artifactId>
			<version>${pom.version}</version>
		</dependency>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>mes-plugins-dictionary-management</artifactId>
			<version>${pom.version}</version>
		</dependency>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>mes-plugins-user-management</artifactId>
			<version>${pom.version}</version>
		</dependency>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>mes-plugins-plugin-management</artifactId>
			<version>${pom.version}</version>
		</dependency>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>mes-plugins-products</artifactId>
			<version>${pom.version}</version>
		</dependency>
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>jstl</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.tiles</groupId>
			<artifactId>tiles-jsp</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.tiles</groupId>
			<artifactId>tiles-core</artifactId>
		</dependency>
		<dependency>
			<groupId>postgresql</groupId>
			<artifactId>postgresql</artifactId>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.mortbay.jetty</groupId>
				<artifactId>maven-jetty-plugin</artifactId>
				<configuration>
					<systemProperties>
            			<systemProperty>
              				<name>com.qcadoo.mes.application.copyResources</name>
               				<value>false</value>
            			</systemProperty>
					</systemProperties>
					<webAppConfig>
						<contextPath>/</contextPath>
			            <baseResource implementation="org.mortbay.resource.ResourceCollection">
							<resourcesAsCSV>
								${basedir}/src/main/webapp;
								${basedir}/../mes-plugins/mes-plugins-crud/src/main/resources/META-INF;
								${basedir}/../mes-plugins/mes-plugins-plugin-management/src/main/resources/META-INF;
								${basedir}/../mes-plugins/mes-plugins-products/src/main/resources/META-INF
							</resourcesAsCSV>
			            </baseResource>
					</webAppConfig>
		        </configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>tomcat</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>1.5</version>
						<executions>
							<execution>
								<id>deploy-to-tomcat</id>
								<phase>install</phase>
								<configuration>
									<target>
										<copy file="${basedir}/target/${pom.artifactId}-${pom.version}.war"
											tofile="/Users/tomcat/deploy/${pom.artifactId}.war" />
									</target>
								</configuration>
								<goals>
									<goal>run</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>