package com.qcadoo.mes.minimalAffordableQuantity;

import org.springframework.stereotype.Service;

@Service
public class QuantityService {

    /*
     * @Autowired private DataDefinitionService dataDefinitionService; public void checkMinimalAffordableQuantity(final
     * ViewDefinitionState viewDefinitionState) { System.out.println("xxx dziala"); FieldComponent plannedQuantity =
     * (FieldComponent) viewDefinitionState.getComponentByReference("plannedQuantity"); FieldComponent technologyLookup =
     * (FieldComponent) viewDefinitionState.getComponentByReference("technology"); Entity technology =
     * dataDefinitionService.get("technologies", "technology").get((Long) technologyLookup.getFieldValue()); if (technology ==
     * null || technology.getId() == null) { return; } Entity technologyEntity = dataDefinitionService.get("technologies",
     * "technology").get(technology.getId()); System.out.println("***" + technologyEntity.getField("quantity"));
     * System.out.println("***111" + plannedQuantity); }
     */

}
